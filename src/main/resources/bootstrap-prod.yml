spring:
  application:
    name: alu-service-sso
  thymeleaf:
    cache:  false
    mode: LEGACYHTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
  datasource:
    username: aluba
    password: Lly48659714!
    url: jdbc:mysql://120.48.17.53:3306/world?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
      #type: com.alibaba.druid.pool.DruidDataSource
    # 下面为连接池的补充设置，应用到上面所有数据源中
    # 初始化大小，最小，最大 127.0.0.1
    initial-size: 5
    min-idle: 5
    max-active: 20

server:
  port: 8503


mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: 1
  mapper-locations: classpath:mapper/*.xml

feign:
  hystrix:
    enabled: true
